return {
	-- "keaising/im-select.nvim",
	-- config = function()
	-- 	require("im_select").setup({
	-- 		-- "keyboard-us" is usually the key for English in Fcitx5
	-- 		default_im_select = "keyboard-us",

	-- 		-- Set to 1 if you want to restore Vietnamese when you press 'i' again
	-- 		-- Set to 0 if you always want English when you start typing
	-- 		default_command = "fcitx5-remote",

	-- 		-- Events to trigger the switch
	-- 		set_default_events = { "VimEnter", "FocusGained", "InsertLeave", "CmdlineLeave" },

	-- 		-- Async events to prevent lag
	-- 		async_events = { "InsertLeave", "BufEnter" },
	-- 	})
	-- end,
}
