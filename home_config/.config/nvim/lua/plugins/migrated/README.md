# Migrated direction

This is the directory for containing plugins that has been replaced and migrated to a new plugin.
These plugins configuration are kept for refernces and historical purposes.

If there are any desires to use these migrated plugins, either copying the config file down a level into the `./lua/plugins/` directory or adding the plugin to the `plugins` table in the main `init.lua` file.
